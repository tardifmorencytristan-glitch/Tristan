# Ω Scientific Writing Compiler — R1

Status: PROVISIONAL / BENCHMARK-INTEGRATION

R1 turns the existing Tristan R4 Living Thesis from a static PDF snapshot into a machine-readable benchmark target.

## Added in R1

- `r4_delta_parser.py`: parses structured `D#` crystallization blocks into typed JSON.
- `benchmarks/r4_living_delta_manifest.json`: frozen metadata for the observed R4 snapshot.
- parser tests including a positive structured block and a negative unstructured case.
- R4 benchmark provenance and explicit epistemic boundary.

## R4 benchmark source

Observed Drive artifact: `These_Tristan_R4_Living_Integrale_2026-09-15.pdf`, snapshot 2026-09-15 23:25 EDT.

The artifact explicitly states `Generated != Verified`, `Projection != Canon`, `RepositoryPASS != ScientificPASS`, and `Capability != Authority`. R1 preserves these invariants and does not upgrade any engineering result into a scientific claim.

## R1 purpose

The next compiler layer is:

`R4 text -> Delta objects -> Claim/Evidence/Supersession graph -> ScientificIR -> lint -> thesis/journal views`.

R1 currently implements the first transition and freezes the benchmark identifiers needed for reproducible work.

## Remaining gates

- full-text R4 ingestion artifact generated reproducibly rather than manually summarized;
- Claim/Evidence/Supersession conversion;
- official Polytechnique LaTeX template build;
- PDF readback;
- dimensional algebra;
- citation semantic support checks;
- figure/data/script hashes;
- baseline R4 vs generated R5 comparison under frozen criteria.
