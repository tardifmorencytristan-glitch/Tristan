# Ω Scientific Writing Compiler R4.2 — LaTeX/PDF/readback gate

R4.2 extends the evidence-bounded manuscript path into a real rendered PDF artifact.

## Fresh-main lineage

The first R4.2 feature branch was created from `ae23ac487157c80227cf9159e987a90b86a05a21`, then `main` advanced independently to `72e7b9ba00ad9b185db88d790adbb89db83e0e26`. This branch is the exact-content replay on that fresher base. No scientific or PDF result was changed during the replay.

## Input

The frozen Battery-T packet remains tied to:

- `tardifmorencytristan-glitch/Tristan-Tardif-Morency`
- commit `d759105682704f037f21fef8f6d396b4d0408f3a`
- `battery/evidence/CALCE_ZERO_FIT_TRANSFER_R0_4.json`

## Pipeline

`Battery evidence packet -> conservative LaTeX renderer -> pdfTeX -> PDF -> image render -> visual inspection -> text readback -> receipt`

The renderer preserves the three mean RMSE values, the 38-45% capacity mismatch, the bounded three-cell scope, the exact evidence anchor, and explicit non-promotion language.

## Failure preserved

The first PDF render failed the visual court because the exact provenance string overflowed the right margin. The renderer was changed from an unbreakable `texttt` anchor to `xurl`/`url` rendering and the PDF was rebuilt and reinspected. The corrected two-page artifact has no observed clipping, overlap, or broken glyphs.

## Boundaries

- `GeneratedPDF != IndependentEvidence`
- `PDFArtifactPASS != ScientificPASS`
- `ReadbackPASS != CitationCourtPASS`
- `LocalBuildPASS != CrossPlatformBuildPASS`
- `R4.2 != WritingSuperiority`

## Next gates

1. exact-head CI for renderer/tests;
2. preserve the derived PDF as a sidecar artifact;
3. run independent/multi-generator writing evaluation;
4. add citation/source courts and accessibility/target-profile checks before any submission-ready status.
